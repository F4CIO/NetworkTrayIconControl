<Window x:Class="CraftSynth.NetworkTrayIconControl.Views.TrayIconView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="CraftSynth Network Tray Icon Control"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Width="1"
        Height="1">

    <tb:TaskbarIcon x:Name="TrayIcon"
                    ToolTipText="{Binding TooltipText, UpdateSourceTrigger=PropertyChanged}"
                    DoubleClickCommand="{Binding OpenNetworkSettingsCommand}" />
</Window>
