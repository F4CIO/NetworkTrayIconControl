<Window x:Class="CraftSynth.NetworkTrayIconControl.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About" Height="200" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtAppName" FontSize="16" FontWeight="Bold" />
        <TextBlock x:Name="txtVersion" Grid.Row="1" Margin="0,6,0,0" />
        <TextBlock x:Name="txtDescription" Grid.Row="2" TextWrapping="Wrap" Margin="0,8,0,0" />
        <TextBlock Grid.Row="3">
            <Hyperlink x:Name="lnkWebsite" NavigateUri="http://www.f4cio.com/NetworkTrayIconControl" RequestNavigate="Hyperlink_RequestNavigate">http://www.f4cio.com/NetworkTrayIconControl</Hyperlink>
        </TextBlock>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="75" IsDefault="True" Click="OkButton_Click">OK</Button>
        </StackPanel>
    </Grid>
</Window>
