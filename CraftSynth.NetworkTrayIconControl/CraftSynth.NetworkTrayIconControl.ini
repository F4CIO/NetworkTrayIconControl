[Network]
; URL used to verify internet access (same endpoint Windows NCSI uses).
; Must return HTTP 200 to be considered reachable.
ConnectivityCheckUrl=http://www.msftconnecttest.com/connecttest.txt

[UI]
; When true, enabling an adapter from the tray menu disables all other adapters.
KeepOnlySingleAdapterEnabled=true
